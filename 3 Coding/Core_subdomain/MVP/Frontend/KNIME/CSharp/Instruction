⚠️ Важный момент (частая ошибка)
Если ты используешь:
    --no-build
👉 C# проект должен быть уже собран хотя бы 1 раз:
    dotnet build C:\MyProject




<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
